# 背包dp

[toc]



## 1.01背包

例题中已知条件有第 $\text{i}$ 个物品的重量$w_i$，价值 $v_{i}$，以及背包的总容量 $$w_{i}$

设$dp$状态$f_{i,j}$为在只能放前 $i$ 个物品的情况下，容量为 $j$ 的背包所能达到的最大总价值．

考虑转移．假设当前已经处理好了前 $i-1$ 个物品的所有状态，那么对于第 $i$ 个物品，当其不放入背包时，背包的剩余容量不变，背包中物品的总价值也不变，故这种情况的最大价值为$f_{i-1,j}$；当其放入背包时，背包的剩余容量会减小$w_{i}$背包中物品的总价值会增大$v_{i}$，故这种情况的最大价值为 $f_{i-1,j-w_{i}}+v_{i}$

由此可以得出状态转移方程：

$$
f_{i,j}=max(f_{i-1,j},f_{i-1,j-w_{i}}+v_{i})
$$

### 实现方式

1.**采用二维dp数组**

- 维护难度低
- 但空间消耗大，容易$MLE$

```c++
for(int i=1;i<=n;i++){
    for(int j=w[i];j<=W;j++){
    f[i][j]=max(f[i-1][j],f[i-1][j-w[i]]+v[i]);
    }
}
```

2.**采用一维滚动数组**

- 空间消耗小
- 采用实时更新
- 但需注意要自右往左维护，否则就会称为完全背包

```c++
for(int i=1;i<=n;i++){
    for(int j=W;j>=w[i];j--){
    	f[j]=max(f[j],f[j-w[i]]+v[i]);
    }
}
```

## 2.完全背包

**完全背包就是 01背包 的升级版，每个物品从只有一个变成有无限个**

**同理 01背包 ，但是递推公式变成了 $f_{j}=max(f_{j},f_{j-w_{i}}+v_{i})$ ，cnt 为取的物品的个数**(***自左往右维护***）

```c++
	for(int i=0;i<m;i++)
	{
		for(int j=w[i];j<=n;j++)
		{
			if(j>=w[i]) //防止越界（当然可以不写，循环条件里写过了）
                dp[j]=max(dp[j],dp[j-w[i]]+v[i]);
		}
	}
```

## 3.多重背包

**同上，每个物品有有限个**

***同理 01背包 ，但是递推公式变成了 $f_{i}=max(f_{i},f_{i}−cnt\times w_{i} +cnt\times v_{i})$ ，cnt 为取的物品的个数***

```c++
for (int i = 1; i <= n; i++) {
  for (int weight = W; weight >= w[i]; weight--) {
    // 多遍历一层物品数量
    for (int k = 1; k * w[i] <= weight && k <= cnt[i]; k++) {
      dp[weight] = max(dp[weight], dp[weight - k * w[i]] + k * v[i]);
    }
  }
}
```

## 4.混合背包

***混合背包就是将前面三种的背包问题混合起来，有的只能取一次，有的能取无限次，有的只能取 k 次***

**递推同完全背包**

```
for (循环物品种类) {
  if (是 0 - 1 背包)
    套用 0 - 1 背包代码;
  else if (是完全背包)
    套用完全背包代码;
  else if (是多重背包)
    套用多重背包代码;
}
```

```c++
for(int i=1;i<=n;i++){
	for(int j=W;j>=w[i];j--){
        int x=0;
		if(c[i]==0) x=j/w[i];//如果有无限个
		else x=min(j/w[i],c[i]);
		for(int k=1;k<=x;k++){
			f[j]=max(f[j],f[j-k*w[i]]+k*v[i]);
		}
	}
}
```

## 5.分组背包

- **一般是每个物品都属于对应的组，每个组最多只能选一个，求最大价值**

```c++
for (int i = 1; i <= n; i++) // 第一层：遍历组
 	for (int j = m; j >= 0; j--) // 第二层：逆序遍历容量
 		for (int k = 0; k < group[i].size(); k++) // 第三层：遍历组内物品
 			if (j >= w[i][k]) dp[j] = max(dp[j], dp[j - w[i][k]]+ v[i][k]); 
```

## 6.二维费用背包

***二维费用的背包问题是指：对于每件物品，具有两种不同的费用，选择这件物品必 须同时付出这两种费用。对于每种费用都有一个可付出的最大值（背包容量）。问怎样 选择物品可以得到最大的价值。 设第i件物品所需的两种费用分别为 $C_{i}$ 和 $D_{i}$ 。两种费用可付出的最大值（也即两 种背包容量）分别为 $V$ 和 $U$ 。物品的价值为$W_{i}$***.

$$
f_{i,u,v}=\max(f_{i-1,u,v},f_{i-1,u-c_{i},v-{d_{i}}}+w_{i})
$$

## 7.方案数背包

1.***对于给定的一个背包容量、物品费用、其他b关系等的问题，求装到一定容量的方案总数．这种问题就是把求最大值换成求和即可，例如 0-1 背包问题的转移方程就变成了：***

$$
dp_{j}\leftarrow dp_{j}+dp_{j-c_{i}}(j>c_{i})
$$

初始条件：$dp_{0}=1$

2.***最佳方案数***：特别的方案数背包

 **状态定义**

- $f_{i,j}$ 表示 **只考虑前 $i$ 个物品** 时，背包容量 **恰好为 $j$** 时能得到的 **最大总价值**。
- $g_{i,j}$ 表示 **只考虑前 $i$ 个物品** 时，背包容量 **恰好为 $j$** 时，能达到最大总价值的 **方案总数**。

（假设物品 $i$ 的体积为 $v$，价值为 $w$）

**转移方程**

对于第 $i$ 个物品（$i \ge 1$）和背包容量 $j$，我们比较两种策略：

1. **不选第 $i$ 个物品**：从 $f_{i-1,j}$ 转移。
2. **选第 $i$ 个物品**（要求 $j \ge v$）：从 $f_{i-1,j-v} + w$ 转移。

根据两种策略得到的最优价值比较，更新 $f_{i,j}$ 和 $g_{i,j}$：

***情况 1：不选更优***

$$
\begin{align}
\text{若}f_{i,j}=f_{i-1,j}&\quad且\quad f_{i,j}\neq f_{i-1,j-v}+w\\
 &g_{i,j}=g_{i-1,j}
\end{align}
$$

***情况 2：选了更优***

$$
\begin{align}
\text{若}f_{i,j} \neq f_{i-1,j}&\quad且\quad f_{i,j}=f_{i-1,j-v}+w\\
 &g_{i,j}=g_{i-1,j-v}
\end{align}
$$

***情况3：一样优***

$$
\begin{align}
\text{若}f_{i,j} = f_{i-1,j}&\quad且\quad f_{i,j}=f_{i-1,j-v}+w\\
 &g_{i,j}=g_{i-1,j-v}
\end{align}
$$

***初始化***

- $f_{0,0} = 0$，$g_{0,0} = 1$（什么都不选，容量恰好为 0 是一种方案）。
- 对于 $j > 0$，$f_{0,j} = -\infty$（表示不可行），$g_{0,j} = 0$。
- 在递推过程中，若 $f$ 值为 $-\infty$，则对应的 $g$ 值为 0。

***这样，最终 $g_{n,m}$ 就是在容量 $m$ 下达到最大总价值的方案总数。***

```c++
for (int i = 0; i < N; i++) {
  for (int j = V; j >= v[i]; j--) {
    int tmp = std::max(dp[j], dp[j - v[i]] + w[i]);
    int c = 0;
    if (tmp == dp[j]) c += cnt[j];                       // 如果从dp[j]转移
    if (tmp == dp[j - v[i]] + w[i]) c += cnt[j - v[i]];  // 如果从dp[j-v[i]]转移
    dp[j] = tmp;
    cnt[j] = c;
  }
}
int max = 0;  // 寻找最优解
for (int i = 0; i <= V; i++) {
  max = std::max(max, dp[i]);
}
int res = 0;
for (int i = 0; i <= V; i++) {
  if (dp[i] == max) {
    res += cnt[i];  // 求和最优解方案数
  }
}
```

***3.第k优解***

***普通的 0-1 背包是要求最优解，在普通的背包 DP 方法上稍作改动，增加一维用于记录当前状态下的前 k 优解，即可得到求 0-1 背包第 k 优解的算法． 具体来讲：$dp_{i,j,k}$ 记录了前 i 个物品中，选择的物品总体积为 j 时，能够得到的第 𝑘 大的价值和．这个状态可以理解为将普通 0-1 背包只用记录一个数据的 $dp_{i,j}$ 扩展为记录一个有序的优解序列．转移时，普通背包最优解的求法是 $dp_{i,j} =max(dp_{i−1,j},dp_{i−1,j−v_{i}} +𝑤_{i}$)，现在我们则是要合并 $dp_{i-1,j}$，$dp_{i-1,j-v_{i}}+w_{i}$这两个大小为 𝑘 的递减序列，并保留合并后前 k 大的价值记在 $dp_{i,j}$里，这一步利用双指针法，复杂度是 $o(mk)$．空间上，此方法与普通背包一样可以压缩掉第一维，复杂度是 $𝑂(𝑚𝑘)$ 的．***

```c++
memset(dp, 0, sizeof(dp));
int i, j, p, x, y, z;
scanf("%d%d%d", &n, &m, &K);
for (i = 0; i < n; i++) scanf("%d", &w[i]);
for (i = 0; i < n; i++) scanf("%d", &c[i]);
for (i = 0; i < n; i++) {
    for (j = m; j >= c[i]; j--) {
        for (p = 1; p <= K; p++) {//往左更新，01背包一维写法
            a[p] = dp[j - c[i]][p] + w[i];//待定数组
            b[p] = dp[j][p];//待定数组
        }//增加维度的01背包，变为了有序序列。
        a[p] = b[p] = -1;//设置哨兵边界
        x = y = z = 1;
        while (z <= K && (a[x] != -1 || b[y] != -1)) {//更新dp，双指针法，要更新到第k个，本来01只需两个数据比大小即可。
            if (a[x] > b[y])
                dp[j][z] = a[x++];
            else
                dp[j][z] = b[y++];
            if (dp[j][z] != dp[j][z - 1]) z++;//去重
        }
    }
}
printf("%d\n", dp[m][K]);
```
