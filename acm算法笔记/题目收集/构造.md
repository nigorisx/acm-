# 构造

[toc]

## p1 [B. ABAB Construction][#https://codeforces.com/contest/2202/problem/B]

> [!note]
>
> > ***题目描述***
> >
> > There is a string $T$ of length $n$, such that $T_i=$ 'a' for all odd $i$ and $T_i=$ 'b' for all even $i$. One day, Bob generated a string $S$ with the following algorithm. 
> >
> > 1. Initialize $S$ to the empty string. 
> >
> > 2. Remove either the **first** letter or the **last** letter from $T$, and **append** it to $S$. 
> >
> > 3. If $T$ is empty, terminate and return the string $S$. Otherwise, go back to the second step. 
> >
> >   Then, Bob wrote down the generated string $S$ on a note and forgot about it for a few years. The note was worn out when Bob found it, and someone might have secretly changed some letters. Of course, Bob wants to know if someone did alter the string! 
> >
> >   You are given a string $X$ of length $n$, which consists of 'a', 'b', and '?'. 
> >
> >   Please determine if there exists a string $A$ which satisfies the following conditions:
> >
> >   - $|A|=n$; 
> >   - $A_i$ is either 'a' or 'b' for all $1 \le i \le n$; 
> >   - $A_i=X_i$ for all $1 \le i \le n$ such that $X_i$ is not '?'; 
> >   - The string $A$ can be generated from the algorithm described above.
>
> > ***思路***
> >
> > ~~首先，我是sb~~，这道题思路本身不难，但我却想不出来。奇数和偶数本身是同源的，因为奇偶可以通过一次操作统一。奇数的第一个一定是a，然后后面就是一个偶数长度的处理。又可观察发现，偶数是不可能的出现aa或bb。只有可能为ab或ba。所以只要去判断是否有aa或bb即可。我看出了偶数的这个规律，但没考虑奇偶同源。
>
> code
>
> >膜拜大佬代码
> >
> >```c++
> >#include <bits/stdc++.h>
> >
> >#define multicase() \
> >    int t;          \
> >    cin >> t;       \
> >    while (t--)
> >using namespace std;
> >using ll = long long;
> >using pii = pair<ll, ll>;
> >
> >ll n;
> >string str;
> >
> >void solve() {
> >    cin >> n;
> >    cin >> str;
> >    if (n % 2 != 0) {
> >        if (str[0] == 'b') {
> >            cout << "NO";
> >            return;
> >        }
> >
> >        for (int i = 1; i < n; i += 2) {
> >            if (str[i] != '?' && str[i + 1] != '?' && str[i] == str[i + 1]) {
> >                cout << "NO";
> >                return;
> >            }
> >        }
> >    } else {
> >        for (int i = 0; i < n; i += 2) {
> >            if (str[i] != '?' && str[i + 1] != '?' && str[i] == str[i + 1]) {
> >                cout << "NO";
> >                return;
> >            }
> >        }
> >    }
> >
> >    cout << "YES";
> >}
> >
> >int main() {
> >    multicase() {
> >        solve();
> >        cout << "\n";
> >    }
> >    return 0;
> >}
> >
> >```
> >
> >

## p2.D. Table Cut

> [!note]
>
> >题目描述
> >
> >Given a table of size n×m, where each cell contains either 0 or 1. The task is to divide it into two parts with a cut that goes from the top left corner to the bottom right corner. The cut lines can only go right or down.
> >
> >Let a be the number of ones in one part of the table after the cut, and b be the number of ones in the other part of the table. The goal is to maximize the value of a⋅b.
>
> >code
> >
> >```c++
> >#include <bits/stdc++.h>
> >using namespace std;
> >#define int long long 
> >#define endl "\n"
> >void solve()
> >{
> >    int n,m;
> >    cin>>n>>m;
> >    int cnt=0;
> >    vector<pair<int, int>> ones;
> >    for(int i=1;i<=n;i++)
> >    {
> >        for(int j=1;j<=m;j++)
> >        {
> >            int x;
> >            cin>>x;
> >            if(x==1){
> >                cnt++;
> >                ones.emplace_back(i,j); //构造数对
> >            }
> >        }
> >    }
> >    sort(ones.begin(), ones.end(), [](const pair<int, int>& a, const pair<int, int>& b) {
> >        if(a.first == b.first) {
> >            return a.second > b.second;
> >        }
> >        return a.first < b.first;
> >    });//按要求排序
> >    
> >    cout<<cnt/2*(cnt-cnt/2)<<endl;
> >    int num=cnt/2;
> >    int sum=0;int idx,idy;
> >    if(cnt == 0 || num == 0){
> >        idx = 1;
> >        idy = 1;
> >    }else{
> >        idx=ones[ones.size()-num].first;
> >        idy=ones[ones.size()-num].second;
> >    }
> >    
> >    int r1=m,d1=n;
> >    string ans="";
> >    for(int i=1;i<=idx-1;i++)
> >    {
> >        ans+="D";
> >        d1-=1;
> >    }
> >    for(int i=1;i<=idy;i++)
> >    {
> >        ans+="R";
> >        r1--;
> >    }
> >    ans+="D";
> >    d1--;
> >    for(int i=1;i<=r1;i++)
> >    {
> >        ans+="R";
> >    }
> >    for(int i=1;i<=d1;i++)
> >    {
> >        ans+="D";
> >    }
> >    cout<<ans<<endl;
> >}
> >signed main()
> >{
> >    ios::sync_with_stdio(0);
> >    cin.tie(0);cout.tie(0);
> >    int t;
> >    cin>>t;
> >    while(t--)
> >    {
> >        solve();
> >    }
> >}
> >```
> >
> >
